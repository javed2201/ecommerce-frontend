import React from 'react'

const Button = ({title}) => {
  return (
    <button className='bg-primary py-4 w-[200px] font-dm font-bold text-sm text-white'>{title}</button>
  )
}

export default Button